<view class="tag-wrapper custom-tag-class" style="background-color:{{ bgColor }}" catchtap="handleTagClick">
  <block wx:if="{{ showIcon }}">
    <image src="{{ iconPath }}"></image>
  </block>
  {{ title }}
</view>