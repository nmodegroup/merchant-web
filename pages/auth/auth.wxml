<!--pages/auth/auth.wxml-->
<view class="auth-page">
    <nav-bar title="Night Mode"></nav-bar>
    <image></image>
    <button class="btn-auth" wx:if="{{visibleAuthBtn}}" open-type="getUserInfo"
        bindgetuserinfo="getUserInfoCallback">免费注册</button>
    <toast id="toast"></toast>
</view>